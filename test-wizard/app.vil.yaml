# test-wizard — VIL Project
# Generated by: vil init test-wizard --template blank
#
# Edit this file, then:
#   vil compile --from yaml --input app.vil.yaml --release
#   vil run --file app.vil.yaml

vil_version: "6.0.0"
name: test-wizard
port: 8080
token: shm

# Add your nodes here:
# nodes:
#   my_sink:
#     type: http-sink
#     port: 8080
#     path: /api
#   my_source:
#     type: http-source
#     url: "http://localhost:18081/api/v1/credits/stream"
#     format: sse

# Add routes:
# routes:
#   - from: my_sink.trigger_out
#     to: my_source.trigger_in
#     mode: LoanWrite
