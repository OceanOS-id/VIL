[package]
name = "vil_llm_cache"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
readme = "README.md"
description = "VIL Semantic Response Cache — exact + embedding-based similarity caching for LLM responses"

[dependencies]
vil_embedder = { version = "0.3", path = "../vil_embedder" }
vil_sdk = { version = "0.3", path = "../vil_sdk" }
vil_server_core = { version = "0.3", path = "../vil_server_core" }
vil_server = { version = "0.3", path = "../vil_server" }
vil_macros = { version = "0.3", path = "../vil_macros" }
axum = { version = "0.7", features = ["macros"] }
serde = { workspace = true }
serde_json = { workspace = true }
dashmap = { workspace = true }
parking_lot = "0.12"

[dev-dependencies]
