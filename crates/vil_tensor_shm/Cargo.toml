[package]
name = "vil_tensor_shm"
description = "Zero-copy tensor serving via SHM-mapped buffers — eliminates serialization between inference stages"
edition.workspace = true
version.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
parking_lot = "0.12"
vil_macros = { path = "../vil_macros" }
vil_server_core = { path = "../vil_server_core" }
vil_sdk = { path = "../vil_sdk" }
vil_server = { path = "../vil_server" }
axum = { version = "0.7", features = ["macros"] }

[dev-dependencies]
