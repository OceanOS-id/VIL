[package]
name = "vil_index_updater"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "N06 — Incremental Index Update: write-ahead log and batched index flushing"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
parking_lot = "0.12"

vil_sdk = { path = "../vil_sdk" }
vil_server_core = { path = "../vil_server_core" }
vil_server = { path = "../vil_server" }
vil_macros = { path = "../vil_macros" }
axum = { version = "0.7", features = ["macros"] }