[package]
name = "vil_jwt"
version.workspace = true
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
serde_json = "1.0"
jsonwebtoken = "9"
serde = { version = "1.0", features = ["derive"] }
