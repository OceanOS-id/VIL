[package]
name = "vil_viz"
version = "0.1.0"
edition = "2021"
description = "VIL workflow visualization engine — graph rendering in 6 formats"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
