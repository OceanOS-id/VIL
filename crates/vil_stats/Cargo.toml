[package]
name = "vil_stats"
description = "VIL Expression function — statistical aggregation (mean, median, percentile)"
repository = "https://github.com/AdeVastra/VIL"
version.workspace = true
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
serde_json = "1.0"
