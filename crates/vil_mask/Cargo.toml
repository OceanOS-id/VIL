[package]
name = "vil_mask"
description = "VIL Expression function — data masking (PII, credit card)"
repository = "https://github.com/AdeVastra/VIL"
version.workspace = true
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
serde_json = "1.0"
regex = "1"
