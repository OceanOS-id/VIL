[package]
name = "vil_id_gen"
description = "VIL Expression function — ID generation (UUID, ULID, snowflake)"
repository = "https://github.com/AdeVastra/VIL"
version.workspace = true
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
serde_json = "1.0"
uuid = { version = "1", features = ["v4", "v7"] }
ulid = "1"
rand = "0.8"
