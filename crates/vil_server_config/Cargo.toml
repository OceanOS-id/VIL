[package]
name = "vil_server_config"
version = "0.1.0"
edition = "2021"
description = "VIL Server Configuration — Multi-source config with profiles"
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
toml = "0.8"
tracing = "0.1"
