[package]
name = "vil_db_macros"
version = "0.1.2"
edition = "2021"
description = "VIL DB Macros — derive macros for VlangEntity and vil_repository"
license = "MIT OR Apache-2.0"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1", features = ["full", "extra-traits"] }
quote = "1"
proc-macro2 = "1"
