[package]
name = "vil_trigger_email"
description = "VIL Phase 3 — IMAP IDLE email trigger with mq_log emit on every new message"
edition.workspace = true
version.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
vil_log              = { workspace = true }
vil_connector_macros = { workspace = true }
vil_trigger_core  = { path = "../vil_trigger_core" }
tokio             = { workspace = true }
async-imap        = { version = "0.10", default-features = false, features = ["runtime-tokio"] }
async-native-tls  = "0.5"
tokio-native-tls  = "0.3"
native-tls        = "0.2"
async-trait       = { workspace = true }
