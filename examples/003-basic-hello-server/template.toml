[template]
id = "blank"
title = "REST API Service"
description = "REST API with JSON transform, echo, and health check"
default_port = 8080
default_upstream = ""

[template.replace]
package_name = "vil-basic-hello-server"
port = "8080"
