[template]
id = "blank"
title = "Blank Project"
description = "REST gateway with JSON transform, echo, and health check"
default_port = 8080
default_upstream = ""

[template.replace]
package_name = "vil-basic-hello-server"
port = "8080"
