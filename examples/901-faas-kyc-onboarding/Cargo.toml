[package]
name = "vil-faas-kyc-onboarding"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
vil_hash = { path = "../../crates/vil_hash" }
vil_id_gen = { path = "../../crates/vil_id_gen" }
vil_phone = { path = "../../crates/vil_phone" }
vil_email_validate = { path = "../../crates/vil_email_validate" }
vil_mask = { path = "../../crates/vil_mask" }
serde_json = "1.0"
