[package]
name = "vil-basic-credit-regulatory-slik"
version = "0.1.0"
edition = "2021"
publish = false

# 008 — Credit Regulatory Stream Pipeline (SLIK/OJK, ShmToken pipeline)
[dependencies]
vil_sdk  = { path = "../../crates/vil_sdk" }
serde_json = "1.0"
