[package]
name = "vil-basic-ai-gw-demo"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

# ─────────────────────────────────────────────────────────────────────────────
# Referensi langsung ke source crates VIL (bukan sdk-dist)
# vil_sdk sudah re-export semua yang diperlukan via prelude::*
# ─────────────────────────────────────────────────────────────────────────────
[dependencies]
vil_sdk = { version = "0.4", path = "../../crates/vil_sdk" }
vil_observer = { version = "0.4", path = "../../crates/vil_observer", features = ["runtime"] }
serde_json = "1.0"
