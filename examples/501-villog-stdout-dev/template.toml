[template]
id = "501-villog-stdout-dev"
title = "Villog Stdout Dev"
description = "Villog Stdout Dev"
default_port = 8080
default_upstream = ""
category = "villog"

[template.replace]
package_name = "example-501-villog-stdout-dev"
port = "8080"

[template.test]
type = "standalone"
prereq = "# No server needed — run binary directly"
port = 8080
method = "RUN"
expect_exit = 0
