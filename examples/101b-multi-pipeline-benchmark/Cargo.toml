[package]
name = "vil-multi-pipeline-benchmark"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
vil_sdk = { path = "../../crates/vil_sdk" }
vil_observer = { path = "../../crates/vil_observer" }
serde_json = "1.0"
