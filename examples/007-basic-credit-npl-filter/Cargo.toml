[package]
name = "vil-basic-credit-npl-filter"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

# ─────────────────────────────────────────────────────────────────────────────
# SSE Pipeline Example 6 (006): Credit NPL Stream Filter
# Layer 3 — Full vil_workflow! macro (Decomposed Builder style)
# Upstream: Core Banking Simulator SSE (port 18081)
# ─────────────────────────────────────────────────────────────────────────────
[dependencies]
vil_sdk  = { version = "0.2" }
serde_json = "1.0"
