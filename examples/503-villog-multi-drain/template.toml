[template]
id = "503-villog-multi-drain"
title = "Villog Multi Drain"
description = "Villog Multi Drain"
default_port = 8080
default_upstream = ""
category = "villog"

[template.replace]
package_name = "example-503-villog-multi-drain"
port = "8080"

[template.test]
type = "standalone"
prereq = "# No server needed — run binary directly"
port = 8080
method = "RUN"
expect_exit = 0
