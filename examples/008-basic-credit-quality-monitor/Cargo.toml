[package]
name = "vil-basic-credit-quality-monitor"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

# 007 — Credit Data Quality Monitor (Core Banking SSE, ShmToken pipeline)
[dependencies]
vil_sdk  = { version = "0.2" }
serde_json = "1.0"
