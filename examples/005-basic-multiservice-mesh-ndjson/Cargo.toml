[package]
name = "vil-basic-multiservice-mesh-ndjson"
version = "0.1.0"
edition = "2021"
publish = false

# 004 — Multi-Service Mesh (Core Banking SSE, ShmToken pipeline mode)
[dependencies]
vil_sdk  = { path = "../../crates/vil_sdk" }
serde_json = "1.0"
