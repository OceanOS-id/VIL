[package]
name = "vil-basic-production-fullstack"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
vil_server = { path = "../../crates/vil_server" }
vil_server_config = { path = "../../crates/vil_server_config" }
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
