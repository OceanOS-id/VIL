[target.'cfg(all())']
rustflags = [
    # ── Security-Critical Lints (warn, not deny — too many existing) ──
    "-Wclippy::undocumented_unsafe_blocks",
    "-Wclippy::panic_in_result_fn",
    "-Wclippy::dbg_macro",
]
