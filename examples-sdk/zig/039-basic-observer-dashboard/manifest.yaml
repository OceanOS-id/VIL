vil_version: "6.0.0"
name: observer-demo
port: 8080
token: shm
mode: server

services:
  - name: demo
    prefix: /api/demo
    endpoints:
      - method: GET
        path: /hello
        handler: hello
      - method: POST
        path: /echo
        handler: echo
