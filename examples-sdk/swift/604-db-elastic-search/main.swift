// 604-db-elastic-search — Swift SDK equivalent
// Compile: vil compile --from swift --input 604-db-elastic-search/main.swift --release

let server = VilServer(name: "app", port: 8080)
server.compile()
