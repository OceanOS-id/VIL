// 703-protocol-soap-client — Swift SDK equivalent
// Compile: vil compile --from swift --input 703-protocol-soap-client/main.swift --release

let server = VilServer(name: "app", port: 8080)
server.compile()
