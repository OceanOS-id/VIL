// 802-trigger-fs-watcher — Swift SDK equivalent
// Compile: vil compile --from swift --input 802-trigger-fs-watcher/main.swift --release

let server = VilServer(name: "app", port: 8080)
server.compile()
