// 702-mq-sqs-send-receive — Swift SDK equivalent
// Compile: vil compile --from swift --input 702-mq-sqs-send-receive/main.swift --release

let server = VilServer(name: "app", port: 8080)
server.compile()
