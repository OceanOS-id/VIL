// 801-trigger-cron-basic — Swift SDK equivalent
// Compile: vil compile --from swift --input 801-trigger-cron-basic/main.swift --release

let server = VilServer(name: "app", port: 8080)
server.compile()
