// 804-trigger-cdc-postgres — Swift SDK equivalent
// Compile: vil compile --from swift --input 804-trigger-cdc-postgres/main.swift --release

let server = VilServer(name: "app", port: 8080)
server.compile()
