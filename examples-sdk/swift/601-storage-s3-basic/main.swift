// 601-storage-s3-basic — Swift SDK equivalent
// Compile: vil compile --from swift --input 601-storage-s3-basic/main.swift --release

let server = VilServer(name: "app", port: 8080)
server.compile()
