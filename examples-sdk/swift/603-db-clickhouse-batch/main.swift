// 603-db-clickhouse-batch — Swift SDK equivalent
// Compile: vil compile --from swift --input 603-db-clickhouse-batch/main.swift --release

let server = VilServer(name: "app", port: 8080)
server.compile()
