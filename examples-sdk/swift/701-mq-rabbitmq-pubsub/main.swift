// 701-mq-rabbitmq-pubsub — Swift SDK equivalent
// Compile: vil compile --from swift --input 701-mq-rabbitmq-pubsub/main.swift --release

let server = VilServer(name: "app", port: 8080)
server.compile()
