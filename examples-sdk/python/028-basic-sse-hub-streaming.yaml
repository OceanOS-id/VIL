vil_version: "6.0.0"
name: sse-hub-demo
port: 8080
token: shm
mode: server

services:
  - name: events
    prefix: /api/events
    endpoints:
      - method: POST
        path: /publish
        handler: publish
        impl:
          mode: stub
          response: '{"ok": true}'
      - method: GET
        path: /stream
        handler: stream
        impl:
          mode: stub
          response: '{"ok": true}'
      - method: GET
        path: /stats
        handler: stats
        impl:
          mode: stub
          response: '{"ok": true}'
