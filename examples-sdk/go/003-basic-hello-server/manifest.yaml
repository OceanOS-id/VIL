vil_version: "6.0.0"
name: vil-basic-hello-server
port: 8080
token: shm
mode: server

services:
  - name: gw
    prefix: /api/gw
    endpoints:
      - method: POST
        path: /transform
        handler: transform
        impl:
          mode: stub
          response: '{"ok": true}'
      - method: POST
        path: /echo
        handler: echo
        impl:
          mode: stub
          response: '{"ok": true}'
      - method: GET
        path: /health
        handler: health
        impl:
          mode: stub
          response: '{"ok": true}'
