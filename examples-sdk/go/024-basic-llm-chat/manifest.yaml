vil_version: "6.0.0"
name: llm-chat
port: 8080
token: shm
mode: server

services:
  - name: chat
    prefix: /api/chat
    endpoints:
      - method: POST
        path: /chat
        handler: chat_handler
        impl:
          mode: stub
          response: '{"ok": true}'
