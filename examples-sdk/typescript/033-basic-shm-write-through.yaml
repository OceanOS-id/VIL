vil_version: "6.0.0"
name: realtime-analytics-dashboard
port: 8080
token: shm
mode: server

services:
  - name: catalog
    prefix: /api/catalog
    endpoints:
      - method: POST
        path: /catalog/search
        handler: catalog_search
        impl:
          mode: stub
          response: '{"ok": true}'
      - method: GET
        path: /catalog/health
        handler: catalog_health
        impl:
          mode: stub
          response: '{"ok": true}'
